<resources>

    <!--
        В манифесте у нас прописана поддержка API, начиная с 14, т.ч. поддерживаем
        только дефолтный styles.xml и для v21
    -->




    <style name="AppTheme" parent="BaseTheme">

        <item name="android:colorButtonNormal">@color/lolli_btn</item>

        <item name="android:navigationBarColor">@color/lolli_primary</item>

        <!-- for navigation drawer -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>

    </style>

</resources>
